#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
	int i, j , k;

	printf("%d %d %d\n", i, j, k);
	printf("%d %d\n", (i + j), (i - k));
	printf("%f %d\n", sqrt(i + j), abs(i - k));
}
